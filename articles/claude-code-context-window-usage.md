---
title: "[Claude Code] リアルタイムで現在のコンテキストウィンドウ使用量を把握する方法" # 記事のタイトル
emoji: "🧠" # アイキャッチとして使われる絵文字（1文字だけ）
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["claudecode", "初心者向け", "コンテキストウィンドウ"] # タグ。["markdown", "rust", "aws"]のように指定する
published: true # 公開:true / 非公開:false
---

## はじめに
この記事では、`Claude Code`でリアルタイムに**コンテキストウィンドウの使用量を把握する**方法を解説します。

:::details 参考資料
@[card](https://code.claude.com/docs/ja/statusline)
:::

## 結論

:::message
`.claude/settings.json`に`statusLine`を設定する

```diff json: .claude/settings.json
{
  --- 略 ---
+  "statusLine": {
+    "type": "command",
+    "command": "npx -y ccusage statusline",
+    "padding": 0
+  }
}
```
下記のように**コンテキストウィンドウ使用量**を含めた情報を可視化することが出来る。
```bash
$ claude
──────────────────────────────────────────────────────────────────────────────────────────────────────────
> Try "fix lint errors"
──────────────────────────────────────────────────────────────────────────────────────────────────────────
🤖 Opus 4.5 | 💰 $0.36 session / $0.35 today / $0.35 block (4h 41m left) | 🔥 $3.13/hr | 🧠 26,763 (13%)
```

:::

## 1. `.claude/settings.json`を編集する
:::message alert
`.claude/settings.json`が存在しない場合は、新規作成する
:::

下記のように`statusLine`を追加してください

```diff json: .claude/settings.json
{
  --- 略 ---
+  "statusLine": {
+    "type": "command",
+    "command": "npx -y ccusage statusline",
+    "padding": 0
+  }
}
```

## 2. Claude Code を起動 or 再起動する

下記コマンドを実行して Claude Code を起動する

```bash
claude
```

## 各項目の説明
```bash
🤖 Opus 4.5 | 💰 $0.36 session / $0.35 today / $0.35 block (4h 41m left) | 🔥 $3.13/hr | 🧠 26,763 (13%)
```

| 項目 | 表示例 | 説明 |
|------|--------|------|
| 🤖**Model** | `Opus 4.5` | 現在使用中のClaudeモデル |
| 💰**session** | `$0.36` | 現在のセッション（会話）での累計コスト |
| 💰**today** | `$0.35` | 本日の累計コスト |
| 💰**block** | `$0.35 (4h 41m left)` | 現在の課金ブロック（通常5時間単位）での使用額と残り時間 |
| 🔥**Burn rate** | `$3.13/hr` | 現在の消費ペース（時間あたりのコスト） |
| 🧠**Tokens** | `26,763 (13%)` | 現在のコンテキストウィンドウ使用量（トークン数とウィンドウ全体に対する割合） |
