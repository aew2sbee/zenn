---
title: "私なりの基本情報技術者試験を攻略する考え方" # 記事のタイトル
emoji: "💯" # アイキャッチとして使われる絵文字（1文字だけ）
type: "idea" # tech: 技術記事 / idea: アイデア
topics: ["基本情報技術者試験", "IPA", "初心者向け"] # タグ。["markdown", "rust", "aws"]のように指定する
published: false # 公開設定（falseにすると下書き）
---

## はじめに

2025/12/21 に`基本情報技術者試験`を受験し、無事に合格しました。
![ipa-fe-01](/images/articles/ipa-information-technology-engineer-examination/ipa-fe-01.png)


資格に関する記事の多くは
その時のコツをまとめております。
@[card](https://www.ipa.go.jp/shiken/kubun/fe.html)


:::message alert
`問題の傾向`や`出題内容`は公言する事が禁止されているため、
その内容や質問については回答することは出来ません。
:::








## 1. 科目Aは答えを知らなくても知っている知識で答える
試験本番や出題年度が異なる過去問


```md
問１ 大規模言語モデルを用いた自然言語処理において，事前学習済みのモデルに対して行う，
     ファインチューニングに関する記述として，最も適切なものはどれか。

- ア: 強化学習を行い，最適な結果が得られるようにする。
- イ: 事前学習と同じデータを繰り返し用いて学習を行い，モデルの精度を高めるようにする。
- ウ: 大量のテキストデータを用いて学習を行い，モデルの精度を高めるようにする。
- エ: 特定のデータを用いて追加で学習を行い，目的とするタスクに適用できるようにする。
```

@[card](https://www.ipa.go.jp/shiken/mondai-kaiotu/sg_fe/koukai/2025r07.html)

上記の問題は公式が公開している基本情報技術者試験のサンプル問題の1つです。
`ファインチューニング`についての設問ですね。

今読んでいるあなたが`ファインチューニング`の意味が分からない。
答えがわからないことを前提に解説します。

### 問われている内容を明確にする
1. まず、問題文からキーワードを見つける。
下記のサンプル問題からキーワードを見つけましょう。
```md
問１ 大規模言語モデルを用いた自然言語処理において，事前学習済みのモデルに対して行う，
ファインチューニングに関する記述として，最も適切なものはどれか。
```

私がキーワードと感じた単語はこちらです。
- `大規模言語モデル`
- `事前学習済み`
- `ファインチューニング`

2. 次に、キーワードから連想して情報を増やす。

| キーワード | 連想情報 |
| ---- | ---- |
| 大規模言語モデル | **AI**についての問題だな! |
| 事前学習済み | **学習**ついての問題だな! |
| ファインチューニング | `ファイン`とは元気？**良い？** みたいな意味かな？<br>`チューニング`はギターでチューニングという言葉を聞く。<br>日本語だと **調整する**みたいな意味だったかな？ |

3. 連想情報を整理し再構築します。

`AI`, `学習`, `良い`, `調整する`
↓
`ファインチューニング`=**AIの学習で良い調整すること**を推測できます。


### 選択肢の差分を明確にする
似たような選択肢を並べて回答者を混乱させます。
そこで**違い**を明らかに選択肢の引掛けを回避します。

サンプル問題の選択肢を見ると、
ちょうど **,** で文章が分割でき分類が出来そうです。　
下記の表のようにまとめました。

| 選択肢 | AIに加える条件 | 得られる結果 |
| ---- | ---- | ---- |
| ア | 強化学習を行い | 最適な結果が得られるようにする。 |
| イ | 事前学習と同じデータを繰り返し用いて学習を行い | モデルの精度を高めるようにする。 |
| ウ | 大量のテキストデータを用いて学習を行い | モデルの精度を高めるようにする。 |
| エ | 特定のデータを用いて追加で学習を行い | 目的とするタスクに適用できるようにする。 |


- **強化学習**を行い
- 事前学習と**同じデータを繰り返し**用いて学習を行い
- **大量のテキストデータ**を用いて学習を行い
- **特定のデータ**を用いて追加で学習を行い

1. 次に


## 科目B: 1問5分以内で回答できる練習する。




```md
問1 次のプログラム中の [a] と [b] に入れる正しい答えの組合せを，解答群の中から選べ。
関数 function1 が受け取る引数と，関数 function2 が受け取る引数とが同じとき，
二つの関数は同じ値を返す。ここで，引数 n と引数 m は正の整数であり，
引数 m は引数 n よりも 10 以上大きい数とする。
```

```py
〔プログラム〕
    ○ 整数型: function1(整数型: n, 整数型: m)
    整数型: count ← 0
    整数型: i
    for (iをnからmまで1ずつ増やす)
        if ((i mod 4)が0と等しい)
            count ← count ＋ 1
        endif
    endfor
    return count

    ○ 整数型: function2(整数型: n, 整数型: m)
    整数型: count ← 0
    整数型: tempN ← n
    整数型: i, j
    for ( [a] )
        if ((tempN mod 4)が0と等しい)
            繰返し処理を終了する
        endif
        tempN ← tempN ＋ 1
    endfor
    for ([b] )
        count ← count ＋ 1
    endfor
    return count

```

## 科目B: 脳疲労で問題が頭に入らなくても無理やり入れる
科目Aを終えて、科目Bの最中にどうしても集中力が切れて

試験中
試験は**190分**も集中し続けなければなりません。
しかし、人間の脳は長時間集中できませんが、やらないといけないです。

そこで



## 科目B: 疑似言語という言語を新しく覚える

@[card](https://gihyo.jp/book/2024/978-4-297-14271-1)


## 結論

| 項目               | 内容              |
| ------------------ | ----------------- |
| 勉強期間           | 2025/05 ~ 2025/12 |
| 勉強時間           | 約 90 時間        |
| 解いた総問題数     | 約 500 問         |
| 当時 保有資格      | 情報セキュリティマネジメント試験              |
| 当時のエンジニア歴 | 4 年              |
| 当時の業務担当     | web 開発          |



### 活用した書籍と活用方法


https://gihyo.jp/book/2024/978-4-297-14425-8


@[card](https://gihyo.jp/book/2024/978-4-297-14271-1)


https://www.sbcr.jp/product/4815629397/


https://r.voyager.co.jp/epm/e1_415730_27112024150108/